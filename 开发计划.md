先给结论：

1. 你的单片机程序相比官方固件，主要不足在 OLED、状态显示、参数配置、断线重连、IMU 和里程计标定能力。
2. 你的“建图算法”本身没有明显落后：本地 `cato_2d.lua` 与官方 `fishbot_2d.lua` 基本完全一致，只有注释文字差异。两者使用的都是 Cartographer 2D。
3. 目前更明显的问题在建图链路集成，而不是算法本身：TF 发布、RViz 配置、雷达坐标系命名和 Nav2 参数存在差异或缺失。


## 一、比较基准

我已经下载并检查了官方固件：

- 文件大小：1,777,648 bytes
- SHA256：`49520478a43bb8852ae0b00f62f38176d89f2d02722b829d9a69c075e765b65e`
- 固件内包含版本字符串：`fishbot-motion-control-v1.8.2.alpha`
- 与官方仓库的 `v1.8.2.alpha` 标签、2025-09-25 提交时间相符

官方源码仓库：

- [fishbot_motion_control_microros v1.8.2.alpha](https://github.com/fishros/fishbot_motion_control_microros/tree/v1.8.2.alpha)
- [官方导航仓库 fishbot_nav](https://github.com/fishros/fishbot_nav)

由于 `.bin` 不能直接还原完整源码，所以我采用了“官方固件字符串分析 + 对应版本官方源码”的方式进行比较。

## 二、OLED 程序方面的差距

这是目前最明确、也是最大的差距。

### 官方固件拥有完整 OLED 系统

官方有独立的显示库：

- [官方 `fishbot_display.h`](https://github.com/fishros/fishbot_motion_control_microros/blob/v1.8.2.alpha/lib/Displays/fishbot_display.h)
- [官方 `fishbot_display.cpp`](https://github.com/fishros/fishbot_motion_control_microros/blob/v1.8.2.alpha/lib/Displays/fishbot_display.cpp)

功能包括：

- Adafruit SSD1306
- 128×64 OLED
- I2C 地址 `0x3C`
- SDA/SCL 使用 GPIO18/GPIO19
- 每秒刷新一次
- 显示固件版本
- 显示当前模式
- 显示 WiFi 状态
- 显示 IP、SSID、密码
- 显示电池电压
- 显示线速度、角速度
- 串口模式下显示波特率
- 通过按键切换显示页面
- 通过按键切换 UDP/串口传输模式

官方固件中还存在按钮处理、蓝牙串口、电池 ADC 和状态管理逻辑。

### 你的固件完全没有 OLED 功能

当前单片机工程只有：

- 电机控制
- 编码器读取
- PID
- 运动学
- micro-ROS
- `/cmd_vel`
- `/odom`
- `/joint_states`

本地工程中没有：

- SSD1306 依赖
- OLED 驱动库
- 按键库
- 电池 ADC
- IMU
- 参数保存
- 状态页面
- WiFi 状态显示

可以从本地入口文件看出这一点：[motor_control/src/main.cpp](/home/nbclass/Documents/PlatformIO/Projects/motor_control/src/main.cpp:1)。

因此，OLED 不是“功能较少”，而是当前版本尚未实现。

## 三、固件架构方面的差距

| 功能 | 官方固件 | 你的固件 | 评价 |
|---|---|---|---|
| OLED | 完整实现 | 没有 | 明确缺失 |
| 电池电压 | GPIO34 ADC 读取 | 没有 | 明确缺失 |
| IMU | 发布 `/imu` | 没有 | 明确缺失 |
| 参数保存 | ESP32 Preferences | 固定写死 | 灵活性不足 |
| WiFi 参数 | 可配置 | 固定 SSID、密码 | 调试不方便 |
| Agent 地址 | 可配置 | 固定 IP、端口 | 部署不方便 |
| ROS 节点参数 | 可配置 | 固定节点名、话题名 | 灵活性不足 |
| `/fishbot_config` 服务 | 有 | 没有 | 明确缺失 |
| UART 配置命令 | 有 | 没有 | 明确缺失 |
| UDP/串口切换 | 有 | 只有 WiFi | 功能较少 |
| Agent 断线重连 | 状态机处理 | 当前主要是一次性初始化 | 稳定性不足 |
| `/joint_states` | 官方没有 | 你增加了 | 这是为了 Gazebo 同步，属于你的定制功能，不是缺点 |

官方固件还将以下参数做成了可配置项：

- 轮距
- 轮径
- 减速比
- 编码器脉冲比
- PID 参数
- PID 输出限制
- odom 发布周期
- odom frame
- cmd_vel 话题
- ROS 节点名称
- ROS namespace
- ROS domain ID

而你的运动学参数主要在代码中固定，例如：

- 轮距约 `175 mm`
- 轮子半径 `32 mm`
- 每脉冲距离 `0.1051566 mm`
- 使用 `millis()` 计算时间间隔

官方则使用减速比、脉冲比、轮径计算每脉冲距离，并使用更细的 `micros()` 时间基准。这个差异可能影响高速运动、低速运动和建图时的里程计精度，但具体数值是否正确仍要根据你的实际电机、编码器和轮胎进行标定。

## 四、建图算法本身的比较

本地配置：

[本地 `cato_2d.lua`](/home/nbclass/SRTP/fishbot_ws/src/fishbot_navigation2/config/cato_2d.lua:4)

官方配置：

[官方 `fishbot_2d.lua`](https://github.com/fishros/fishbot_nav/blob/main/src/fishbot_cartographer/config/fishbot_2d.lua)

两者主要参数一致：

- Cartographer 2D
- 使用一个激光雷达
- 使用 odom
- 不使用 IMU
- 使用在线相关性扫描匹配
- `min_range = 0.10`
- `max_range = 3.5`
- `missing_data_ray_length = 3.0`
- `motion_filter.max_angle_radians = 0.1`
- `constraint_builder.min_score = 0.65`
- `global_localization_min_score = 0.7`

所以目前不能说你的建图算法比官方差。更准确的说法是：

> 算法配置基本相同，但你的传感器数据、里程计质量和启动链路还没有达到官方程序的完整程度。

另外，官方固件虽然提供 IMU，但官方 Cartographer 默认配置中：

```lua
TRAJECTORY_BUILDER_2D.use_imu_data = false
```

因此当前官方默认建图流程实际上也没有使用 IMU。

## 五、你的建图链路中已经发现的具体不足

### 1. `robot_state_publisher` 在 mapping launch 中被注释掉

本地文件：

[mapping.launch.py](/home/nbclass/SRTP/fishbot_ws/src/fishbot_navigation2/launch/mapping.launch.py:66)

当前代码是：

```python
# ld.add_action(robot_state_publisher)
```

这意味着单独启动 `mapping.launch.py` 时，不会自动发布 URDF 对应的 TF。

Cartographer 至少需要能够查询：

```text
base_link -> laser_link
odom -> base_link
```

如果其他 bringup 文件没有同时发布这些 TF，雷达数据可能因为无法完成坐标变换而被丢弃。

这是本地建图启动链路中的确定性问题。

### 2. 本地 launch 引用了不存在的 RViz 配置

[mapping.launch.py](/home/nbclass/SRTP/fishbot_ws/src/fishbot_navigation2/launch/mapping.launch.py:26) 中引用了：

```text
config/cartographer.rviz
```

但本地 `src/fishbot_navigation2/config/` 中没有这个文件。

官方仓库中有对应文件：

[官方 `cartographer.rviz`](https://github.com/fishros/fishbot_nav/blob/main/src/fishbot_cartographer/config/cartographer.rviz)

因此本地建图启动时，RViz 可能因为配置文件不存在而报错或使用默认界面。

### 3. 雷达坐标系命名存在历史遗留差异

官方 URDF 同时存在：

```text
laser_link
laser_frame
```

而本地 URDF 只有：

```text
laser_link
```

本地当前参数文件使用的是：

[ydlidar.yaml](/home/nbclass/SRTP/fishbot_ws/src/ydlidar_ros2/params/ydlidar.yaml:4)

```yaml
frame_id: laser_link
```

这与当前本地 URDF 是匹配的，但部分旧 launch、驱动默认值和官方资料仍然使用 `laser_frame`。

这不一定会立即造成错误，但需要统一命名，否则可能出现：

- RViz 中雷达数据不显示
- Cartographer 查询不到对应 TF
- 同时存在两个雷达 frame
- 修改 launch 后突然出现 TF 错误

### 4. Nav2 的障碍物膨胀参数明显小于官方

本地：

[nav2_params.yaml](/home/nbclass/SRTP/fishbot_ws/src/fishbot_navigation2/config/nav2_params.yaml:199)

```yaml
inflation_radius: 0.01
```

官方：

[官方 `nav2_params.yaml`](https://github.com/fishros/fishbot_nav/blob/main/src/fishbot_navigation2/config/nav2_params.yaml)

```yaml
inflation_radius: 0.55
```

本地还将速度限制设为了：

```yaml
max_vel_x: 0.10
max_vel_theta: 0.5
```

官方是：

```yaml
max_vel_x: 0.26
max_vel_theta: 1.0
```

速度降低属于保守调参，不一定是问题；但 `inflation_radius = 0.01` 会大幅减小障碍物安全缓冲区，可能导致规划路径贴近障碍物。这个参数应结合机器人尺寸、雷达误差和实际环境重新测试，不能直接认为更优。

## 六、当前不足的优先级

我建议按以下顺序处理：

### 第一优先级：补齐 OLED 和基础状态系统

目标：

- 显示运行模式
- 显示 WiFi/Agent 状态
- 显示 `/cmd_vel` 当前速度
- 显示编码器速度
- 显示电池电压
- 显示错误状态
- 按键切换页面

实现时应复用官方 `FishBotDisplay` 的设计思路，并遵循你之前确定的规范：

- OLED 代码放在 `lib/Displays/`
- 不把显示逻辑堆到 `main.cpp`
- 新函数放入对应库的 `.h/.cpp`
- 保持详细中文注释
- 不重复实现已有库功能

### 第二优先级：完善固件参数和通信状态管理

建议补充：

- Preferences 参数保存
- WiFi 参数配置
- Agent IP/端口配置
- PID 参数配置
- 轮距、轮径、脉冲参数配置
- Agent 在线检测
- 自动断线重连
- `/fishbot_config` 服务或 UART 配置命令

这部分会直接改善你之前遇到的：

```text
每次 bringup 前需要手动 pkill micro_ros_agent
```

不过上位机 Agent 的进程管理和单片机断线重连应分开处理，不能只依赖手动杀进程。

### 第三优先级：重新标定里程计

需要实际测量：

- 左右轮每脉冲对应距离
- 左右轮直径差异
- 轮距
- 编码器方向
- 左右轮速度比例
- 直线行驶误差
- 原地旋转误差

然后再决定是否迁移官方的：

- `micros()` 时间计算
- 减速比/脉冲比/轮径参数化
- 里程计参数持久化

不要直接照抄官方默认值，因为你的电机接线和轮子安装方向已经与官方存在差异。

### 第四优先级：修正建图启动链路

建议按这个顺序验证：

1. 启动 URDF 和 `robot_state_publisher`
2. 确认 `base_link -> laser_link`
3. 确认 `/odom -> base_footprint`
4. 确认 `/scan` 的 `frame_id`
5. 确认 Cartographer 能收到 `/scan`
6. 确认 `/submap_list`、`/scan_matched_points2` 和 `/map`
7. 最后再调 Nav2 参数

建图算法本身暂时不需要更换。

## 最终判断

你的程序当前不是“算法落后”，而是处于一个比较精简的实验版本：

- 运动控制基本功能已经具备
- Gazebo 所需的 `/joint_states` 是你额外补充的功能
- OLED、IMU、参数系统和断线重连尚未实现
- 里程计参数还没有达到官方的可配置和可标定程度
- Cartographer 配置基本与官方一致
- 建图启动文件和 TF/RViz 链路还需要整理
- Nav2 障碍物膨胀参数需要重新评估

如果下一步正式开发，我建议先做“官方 OLED + 固件状态管理”，然后做“里程计标定”，最后再处理建图和 Nav2 的参数优化。